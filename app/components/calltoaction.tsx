import { bodoniModa } from "../ui/fonts"

export const CallToAction = () => {
    return (
        <div>
            <p className={`${bodoniModa.className} font-bodoni font-normal italic my-2 text-sm lg:text-lg md:text-md`}>Hello! Welcome to my portfolio! In need of a website? An Android mobile app? Look no further!</p>
        </div>
    );
}